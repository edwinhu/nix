{
  "manifest_version": 3,
  "name": "LSEG Workspace — hide unsupported-browser banner",
  "version": "1.0.0",
  "description": "Hides Workspace Web's BROWSER NOT SUPPORTED strip. Chromium is not on LSEG's supported list, but Workspace works; the banner is UA-sniffed noise that costs 44px on every page.",
  "content_scripts": [
    {
      "matches": ["https://workspace.refinitiv.com/*"],
      "css": ["hide.css"],
      "all_frames": true,
      "run_at": "document_start"
    }
  ]
}
